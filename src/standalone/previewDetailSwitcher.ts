/**
 * Preview Detail Switcher Module - Standalone Entry Point
 * Switches between preview and detail views; multiple detail items sharing a
 * detail-id are shown together as a group.
 */

export {
  initPreviewDetailSwitcher,
  initPreviewDetailSwitchers,
} from './previewDetailSwitcher/index';
export type { PreviewDetailSwitcherOptions } from './previewDetailSwitcher/index';
